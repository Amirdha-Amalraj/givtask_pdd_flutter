import 'package:flutter/material.dart';

class SkillQuizScreen extends StatelessWidget {
  const SkillQuizScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('SkillQuizScreen')),
      body: const Center(child: Text('SkillQuizScreen Placeholder')),
    );
  }
}
