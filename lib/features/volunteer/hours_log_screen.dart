import 'package:flutter/material.dart';

class HoursLogScreen extends StatelessWidget {
  const HoursLogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('HoursLogScreen')),
      body: const Center(child: Text('HoursLogScreen Placeholder')),
    );
  }
}
