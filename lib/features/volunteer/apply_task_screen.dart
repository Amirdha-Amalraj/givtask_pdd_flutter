import 'package:flutter/material.dart';

class ApplyTaskScreen extends StatelessWidget {
  const ApplyTaskScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ApplyTaskScreen')),
      body: const Center(child: Text('ApplyTaskScreen Placeholder')),
    );
  }
}
