import 'package:flutter/material.dart';

class SubmitMilestoneScreen extends StatelessWidget {
  const SubmitMilestoneScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('SubmitMilestoneScreen')),
      body: const Center(child: Text('SubmitMilestoneScreen Placeholder')),
    );
  }
}
