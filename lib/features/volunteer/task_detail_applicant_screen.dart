import 'package:flutter/material.dart';

class TaskDetailApplicantScreen extends StatelessWidget {
  const TaskDetailApplicantScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('TaskDetailApplicantScreen')),
      body: const Center(child: Text('TaskDetailApplicantScreen Placeholder')),
    );
  }
}
