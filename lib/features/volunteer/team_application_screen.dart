import 'package:flutter/material.dart';

class TeamApplicationScreen extends StatelessWidget {
  const TeamApplicationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('TeamApplicationScreen')),
      body: const Center(child: Text('TeamApplicationScreen Placeholder')),
    );
  }
}
