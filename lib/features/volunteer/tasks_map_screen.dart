import 'package:flutter/material.dart';

class TasksMapScreen extends StatelessWidget {
  const TasksMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('TasksMapScreen')),
      body: const Center(child: Text('TasksMapScreen Placeholder')),
    );
  }
}
