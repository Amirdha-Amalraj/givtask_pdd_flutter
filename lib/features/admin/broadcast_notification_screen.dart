import 'package:flutter/material.dart';

class BroadcastNotificationScreen extends StatelessWidget {
  const BroadcastNotificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('BroadcastNotificationScreen')),
      body: const Center(child: Text('BroadcastNotificationScreen Placeholder')),
    );
  }
}
