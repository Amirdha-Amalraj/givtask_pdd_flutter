import 'package:flutter/material.dart';

class DisputeModerationScreen extends StatelessWidget {
  const DisputeModerationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('DisputeModerationScreen')),
      body: const Center(child: Text('DisputeModerationScreen Placeholder')),
    );
  }
}
