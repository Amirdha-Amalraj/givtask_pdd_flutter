import 'package:flutter/material.dart';

class TaskDetailEditScreen extends StatelessWidget {
  const TaskDetailEditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('TaskDetailEditScreen')),
      body: const Center(child: Text('TaskDetailEditScreen Placeholder')),
    );
  }
}
