import 'package:flutter/material.dart';

class NgoReviewsScreen extends StatelessWidget {
  const NgoReviewsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('NgoReviewsScreen')),
      body: const Center(child: Text('NgoReviewsScreen Placeholder')),
    );
  }
}
