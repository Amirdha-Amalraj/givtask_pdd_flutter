import 'package:flutter/material.dart';

class ApplicantProfileScreen extends StatelessWidget {
  const ApplicantProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ApplicantProfileScreen')),
      body: const Center(child: Text('ApplicantProfileScreen Placeholder')),
    );
  }
}
