import 'package:flutter/material.dart';

class PostAnnouncementScreen extends StatelessWidget {
  const PostAnnouncementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PostAnnouncementScreen')),
      body: const Center(child: Text('PostAnnouncementScreen Placeholder')),
    );
  }
}
